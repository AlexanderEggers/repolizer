include ':example', ':annotation', ':processor', ':core', ':retrofit', ':livedata'

project(":annotation").name = "repolizer-annotation"
project(":core").name = "repolizer-core"
project(":example").name = "repolizer-example"
project(":processor").name = "repolizer-processor"

project(":retrofit").projectDir = file("../repolizer-adapter/retrofit")
project(":retrofit").name = "repolizer-retrofit"

project(":livedata").projectDir = file("../repolizer-adapter/livedata")
project(":livedata").name = "repolizer-livedata"

rootProject.name = "repolizer-library"
